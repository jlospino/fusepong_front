import React from 'react'

const Tickets = (props) => {
    const parameters = props.location.aboutProps;
    return (
        <div>
            <h4>Tickets History { parameters.id }</h4>
            <h4>Tickets History</h4>
            <h4>Tickets History</h4>
        </div>
    )
}

export default Tickets
